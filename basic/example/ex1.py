# x : x点
# y : y点

# x = -320 < x < 320
# y = -240 < y < 240

class marker:

    ## 計算様リストの作成
    for x in s.split(';'):
        return (x[0],"[ c ]")

    def __init__(self,x,y,angle):
        self.x = x
        self.y = y
        self.angle = angle

    def f(self,length):
        ## 最初は右を向いている
        ## いま向いている方向をどう表す?

    def t(self,param):

        if param == 0:
            pass
        else:
            self.angle += param
    
    def r(rTime):
        for r in rTime:
            # R - E間の処理をループする

