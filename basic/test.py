# 標準入出力、コメント文、変数

# name = input("名前を入力します")
# print("名前は",name,"です")

# format関数: 変数の値(String型)を出力するデバッグ関数。
## 出力したい変数を{}に書き換え、表現したい文章を{}含めてクォーテーションで囲む
### {}'.format(出力したい変数)を記述する
# print('名前は{}'.format(name))


# 複数の変数に、同じ値を同時に入力したい場合の処理
a = b = 'hello'
print(a,b)